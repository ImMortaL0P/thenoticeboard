import { ComingSoon } from "@/components/ComingSoon";

export default function Page() {
  return (
    <ComingSoon title="For employers" phase="Phase 4">
      Private companies register, get verified by an admin (official email domain + GSTIN/CIN), then submit openings that go
      into the admin review queue before publishing.
    </ComingSoon>
  );
}
